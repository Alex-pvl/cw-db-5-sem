+   get_data()
+   sort_date_release() 
+   sort_vendor() 
+   sort_price() 
+   sort_date_sold() 
+   max_price() 
+   min_price() 
+   avg_price() 
+   between_price(numeric, numeric) 
+   get_by_vendor(varchar) 
+   get_less_than(numeric) 
+   get_by_release(date) 
+   by_date(date, date) 
+   by_date_and_vendor(date, date, vendor) 
+   most_popular() 
+   by_vendor_with_price(varchar, text) 
+   greater_than(numeric) 
+   greater_than(numeric, varchar) 
+   avg_price_by_date(date, date) 
+   price_greater_than_avg_manufac(text) 
+   is_regular_delivery()
+   count_with_price_stats(integer)